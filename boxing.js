(()=>{const API='https://img-api-proxy.magsipocarnie.workers.dev',divisionEl=document.getElementById('boxingDivision'),bodyEl=document.getElementById('boxingBody'),searchEl=document.getElementById('boxingSearch'),content=document.getElementById('boxingContent'),statusEl=document.getElementById('boxingStatus'),sourcesEl=document.getElementById('boxingSources'),pager=document.getElementById('boxingPager'),prev=document.getElementById('boxingPrev'),next=document.getElementById('boxingNext'),pageLabel=document.getElementById('boxingPageLabel'),dialog=document.getElementById('boxerDialog'),dialogContent=document.getElementById('boxerDialogContent');let meta={divisions:[],api_divisions:[],organizations:[],sources:[],configured:false},view='rankings',page=1,totalPages=1,searchTimer;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),n=v=>Number.isFinite(Number(v))?Number(v):0,boxerId=x=>x?.fighter_id||x?.id||'',boxerName=x=>x?.fighter_name||x?.name||x?.full_name||'Vacant',orgName=x=>x?.organization?.name||x?.organization_name||x?.org?.name||x?.name||'Organization',orgCode=name=>{const m=String(name||'').toUpperCase().match(/\b(WBC|WBA|IBF|WBO)\b/);return m?m[1]:String(name||'').toUpperCase()};function setStatus(t){statusEl.textContent=t||''}function officialSources(){if(!meta.sources?.length){sourcesEl.innerHTML='';return}sourcesEl.innerHTML='<div class="boxing-feed-note">Official ranking references</div><div class="official-boxing-sources">'+meta.sources.map(s=>'<a class="official-boxing-source" href="'+esc(s.url)+'" target="_blank" rel="noopener noreferrer"><strong>'+esc(s.body)+'</strong><span>'+esc(s.label)+'</span></a>').join('')+'</div>'}function selectedDivision(){return meta.divisions.find(d=>String(d.page)===divisionEl.value)||meta.divisions[0]}function apiDivisionId(){const d=selectedDivision(),m=(meta.api_divisions||[]).find(x=>String(x.name||'').toLowerCase()===String(d?.name||'').toLowerCase());return m?.id||''}async function get(path,params={}){const q=new URLSearchParams(params),r=await fetch(API+path+(q.toString()?'?'+q:''),{cache:'no-store'});if(!r.ok)throw 0;return r.json()}function record(f){const s=f?.stats||{};return[n(s.wins),n(s.losses),n(s.draws)].join('-')}function renderBoxerCard(f){const s=f?.stats||{};return'<button class="boxer-card" type="button" data-boxer-id="'+esc(boxerId(f))+'"><div><h3>'+esc(boxerName(f))+'</h3>'+(f.nickname?'<div class="tag">“'+esc(f.nickname)+'”</div>':'')+'</div><div class="record">'+record(f)+'</div><div class="boxer-meta">'+[f.division?.name,f.nationality,f.stance,s.ko_wins!=null?s.ko_wins+' KO wins':''].filter(Boolean).map(x=>'<span>'+esc(x)+'</span>').join('')+'</div></button>'}function renderSourcesOnly(){content.innerHTML='<div class="boxing-feed-note">Official ranking sources are connected below. The searchable all-boxer directory needs the Boxing Data API key in the Cloudflare Worker.</div>';pager.hidden=true;officialSources()}function renderRankings(data){const selected=bodyEl.value,items=(data.data||[]).filter(x=>!selected||orgCode(orgName(x))===selected);content.innerHTML=items.length?'<div class="boxing-rankings">'+items.map(g=>{const org=orgCode(orgName(g)),ch=g.champions||[],rs=g.rankings||[];return'<section class="boxing-body"><div class="boxing-body-head"><strong>'+esc(org)+'</strong><span>'+esc(g.updated_at||'')+'</span></div>'+(ch.length?'<div class="boxing-champions">'+ch.map(c=>'<div class="boxing-champion"><b>'+esc(c.title_type||'Champion')+'</b><button class="boxer-link" type="button" data-boxer-id="'+esc(boxerId(c))+'">'+esc(boxerName(c))+'</button></div>').join('')+'</div>':'')+'<table class="boxing-table"><thead><tr><th>Rank</th><th>Boxer</th><th>Country</th><th>Status</th></tr></thead><tbody>'+rs.map(r=>'<tr><td>'+esc(r.rank??'—')+'</td><td>'+(r.is_vacant?'Vacant':'<button class="boxer-link" type="button" data-boxer-id="'+esc(boxerId(r))+'">'+esc(boxerName(r))+'</button>')+'</td><td>'+esc(r.nationality||r.country||'—')+'</td><td>'+esc(r.title_type||r.status||'')+'</td></tr>').join('')+'</tbody></table></section>'}).join('')+'</div>':'<div class="empty">No ranking data returned for this division.</div>';pager.hidden=true;officialSources()}function renderFighters(data){const fighters=Array.isArray(data.data)?data.data:[];content.innerHTML=fighters.length?'<div class="boxer-grid">'+fighters.map(renderBoxerCard).join('')+'</div>':'<div class="empty">No boxers found.</div>';updatePager(data.pagination);officialSources()}function fightNames(f){const a=f?.fighters?.fighter_1||{},b=f?.fighters?.fighter_2||{};return[a.full_name||a.name||'Fighter A',b.full_name||b.name||'Fighter B']}function renderFights(data){const fights=Array.isArray(data.data)?data.data:[];content.innerHTML=fights.length?'<div class="fight-grid">'+fights.map(f=>{const[a,b]=fightNames(f),res=f.results||{};return'<article class="fight-card"><div class="fight-date">'+esc(f.date?new Date(f.date).toLocaleDateString():f.status||'')+'</div><h3>'+esc(a)+' vs '+esc(b)+'</h3><div class="boxer-meta">'+[f.status,f.division?.name,f.location].filter(Boolean).map(x=>'<span>'+esc(typeof x==='string'?x:(x.name||''))+'</span>').join('')+'</div>'+(res.outcome?'<div class="fight-result">'+esc(res.outcome_long||res.outcome)+(res.round?' · Round '+esc(res.round):'')+'</div>':'')+'</article>'}).join('')+'</div>':'<div class="empty">No fights returned.</div>';updatePager(data.pagination);officialSources()}function updatePager(p={}){page=Number(p.page||page||1);totalPages=Math.max(1,Number(p.total_pages||1));pager.hidden=totalPages<=1;pageLabel.textContent='Page '+page+' of '+totalPages;prev.disabled=page<=1;next.disabled=page>=totalPages}async function load(){setStatus('Loading');content.innerHTML='<div class="empty">Loading boxing data…</div>';try{if(!meta.configured){renderSourcesOnly();setStatus('');return}if(view==='rankings')renderRankings(await get('/boxing/rankings',{page_num:selectedDivision()?.page||1}));else if(view==='fighters'){const p={page_num:page,page_size:24},q=searchEl.value.trim(),d=apiDivisionId();if(q)p.name=q;if(d)p.division_id=d;renderFighters(await get('/boxing/fighters',p))}else{const p={page_num:page,page_size:24,date_sort:'DESC'},d=apiDivisionId();if(d)p.division_id=d;renderFights(await get('/boxing/fights',p))}setStatus('')}catch(e){content.innerHTML='<div class="empty">Boxing data is temporarily unavailable.</div>';setStatus('Feed unavailable');officialSources()}}async function openBoxer(id){if(!id)return;dialogContent.innerHTML='<div class="empty">Loading boxer…</div>';dialog.showModal();try{const j=await get('/boxing/fighter',{id}),f=j.data||{},s=f.stats||{};dialogContent.innerHTML='<h2>'+esc(boxerName(f))+'</h2>'+(f.nickname?'<div class="tag">“'+esc(f.nickname)+'”</div>':'')+'<div class="boxer-record-row"><div class="boxer-stat"><b>'+n(s.wins)+'</b><span>Wins</span></div><div class="boxer-stat"><b>'+n(s.losses)+'</b><span>Losses</span></div><div class="boxer-stat"><b>'+n(s.draws)+'</b><span>Draws</span></div><div class="boxer-stat"><b>'+n(s.ko_wins)+'</b><span>KO wins</span></div></div><div class="boxer-detail-grid">'+[['Division',f.division?.name],['Nationality',f.nationality],['Stance',f.stance],['Age',f.age],['Height',f.height],['Reach',f.reach],['Pro debut',f.debut],['Total bouts',s.total_bouts]].map(([k,v])=>'<div><small>'+k+'</small>'+esc(v??'—')+'</div>').join('')+'</div>'+(Array.isArray(f.titles)&&f.titles.length?'<div class="boxer-titles">'+f.titles.map(t=>'<span class="boxer-title">'+esc(t.name||t.title||'Title')+'</span>').join('')+'</div>':'')}catch(e){dialogContent.innerHTML='<div class="empty">Boxer profile unavailable.</div>'}}async function init(){try{meta=await get('/boxing/meta');divisionEl.innerHTML=(meta.divisions||[]).map(d=>'<option value="'+d.page+'">'+esc(d.name)+'</option>').join('');officialSources();await load()}catch(e){setStatus('Boxing feed unavailable');content.innerHTML='<div class="empty">Unable to load Boxing Hub.</div>'}}document.querySelectorAll('[data-boxing-view]').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('[data-boxing-view]').forEach(x=>x.classList.toggle('active',x===btn));view=btn.dataset.boxingView;page=1;bodyEl.disabled=view!=='rankings';searchEl.disabled=view==='rankings';load()}));divisionEl.addEventListener('change',()=>{page=1;load()});bodyEl.addEventListener('change',load);document.getElementById('boxingRefresh').addEventListener('click',load);searchEl.addEventListener('input',()=>{clearTimeout(searchTimer);if(view==='fighters'){page=1;searchTimer=setTimeout(load,300)}});prev.addEventListener('click',()=>{if(page>1){page--;load()}});next.addEventListener('click',()=>{if(page<totalPages){page++;load()}});document.addEventListener('click',e=>{const b=e.target.closest('[data-boxer-id]');if(b)openBoxer(b.dataset.boxerId)});dialog.querySelector('.boxer-dialog-close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});init()})();