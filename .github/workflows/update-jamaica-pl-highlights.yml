name: Update Jamaican Premier League highlights
on:
  schedule:
    - cron: "19,49 * * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scripts/football_highlights_common.py"
      - "scripts/update_jamaica_pl_highlights.py"
      - ".github/workflows/update-jamaica-pl-highlights.yml"
permissions:
  contents: write
concurrency:
  group: jamaica-pl-highlights-update
  cancel-in-progress: true
jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Refresh Jamaican Premier League highlights
        env:
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "IMG Jamaican Premier League Highlights Bot"
          git config user.email "actions@users.noreply.github.com"
          for attempt in 1 2 3; do
            git fetch origin main
            git reset --hard origin/main
            python scripts/update_jamaica_pl_highlights.py
            if git diff --quiet -- jamaica-pl-highlights.json; then
              echo "No Jamaican Premier League highlight changes."
              exit 0
            fi
            git add jamaica-pl-highlights.json
            git commit -m "Update Jamaican Premier League highlights"
            if git push origin HEAD:main; then exit 0; fi
            echo "Push raced with another main update; regenerating (attempt $attempt)."
          done
          echo "Unable to publish Jamaican Premier League highlights after retries."
          exit 1
